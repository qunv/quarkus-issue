package org.acme.getting.started;

import org.acme.getting.started.export.ExcelExportUtility;
import org.acme.getting.started.Issue;
import org.apache.poi.ss.usermodel.Cell;
import org.apache.poi.ss.usermodel.CellStyle;
import org.apache.poi.ss.usermodel.CellType;
import org.apache.poi.ss.usermodel.Row;

import java.text.SimpleDateFormat;
import java.util.List;

public class ExportRevisionResponseExcel  {

    /*
     * @see ASTExcelExportUtility#fillData(java.util.List)
     */
//    public void fillData(List<Issue> dataList) {
//        int rowCount = 0;
//        for (Issue user : dataList) {
//            // creating row
//            Row row = sheet.createRow(++rowCount);
//
//            // adding first cell to the row
//            Cell idCell = row.createCell(0);
//            idCell.setCellValue(user.getAlive());
//
//            // adding second cell to the row
//            Cell nameCell = row.createCell(1);
//            nameCell.setCellValue(user.getData());
//        }
//    }

}